{
  "name": "screeps_ants",
  "version": "1.0.0",
  "description": "A screeps bot based of ant colonies",
  
  "scripts": {
      "clean": "rimraf dist/ && rimraf bin/",
      "build": "npm run clean && tsc -p tsconfig.json",
      "push" : "npm run build && grunt",
      
      "test": "echo \"Error: no test specified\" && exit 1"
  },

  "author": "William Dann",
  "license": "MIT",
  
  "devDependencies": {
    "@types/lodash": "^4.14.191",
    "@types/screeps": "^3.3.2",
    "grunt": "^1.5.3",
    "grunt-cli": "^1.4.3",
    "grunt-contrib-clean": "^2.0.1",
    "grunt-contrib-copy": "^1.0.0",
    "lodash": "^4.17.21",
    "rimraf": "^3.0.1",
    "grunt-screeps": "^1.5.0"
  }
}
